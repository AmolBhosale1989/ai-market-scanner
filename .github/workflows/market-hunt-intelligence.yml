name: Market Hunt Intraday Intelligence (Manual Fallback)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Primary intraday intelligence now runs in market-hunt-intraday.yml."
